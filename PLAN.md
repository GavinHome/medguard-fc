# MedGuard-FC：面向中文医疗工具调用场景的分级故障恢复与安全刹车模型

> Medical Guard for Function Calling
> 项目定位：**阶段一（本地验证）**。目标是用 Mac 本地训练的低成本方式，验证一个核心假设——
> "分级故障恢复数据"能同时提升医疗工具调用模型的双轴指标：**任务解决率↑ 危险动作率↓**。
> 验证通过后进入阶段二（租卡强化学习训练），冲刺论文投稿级与工业可部署性。

---

## 1. 解决的细分场景

### 1.1 一句话描述

**中文医疗场景下、私有化部署的小模型 agent，在内部工具 API 调用失败时，如何"既把问题解决掉、又不在危险的地方瞎编"。**

### 1.2 具体场景画像

| 要素 | 描述 |
|---|---|
| 部署环境 | 医院内网 / 互联网诊疗平台 / 健康管理 SaaS——数据不能出门，必须私有化部署小模型（4B-14B） |
| 典型任务 | 智能导诊分诊、用药相互作用查询、检验报告解读辅助（三个工具域，共约 24 个 API schema） |
| 核心痛点 | 生产环境里工具调用失败是常态（内部 API 超时、返回异常、参数被拒）。现有模型失败后要么**幻觉编造**（医疗场景最危险），要么**塌缩成一律"建议就医"**（产品无价值） |
| 合规约束 | 《互联网诊疗管理办法》：AI 不得出具诊断和处方。"知道哪里必须停"是监管硬要求，不是道德姿态 |
| 为什么没人做 | 四轮检索核实：医疗工具调用（MMedAgent，英文，只管选工具）、医疗安全基准（MedSafetyBench / Health-ORSC-Bench，英文，只管问答）、故障恢复训练（PALADIN / Fission-GRPO，英文通用域）、中文医疗评测（CMB / MedBench，只管考试问答）——**"中文 × 医疗工具 × 失败安全"三重交集无占位者**（知网未查，投稿前需正式文献综述） |

### 1.3 核心设计：分级降级光谱（不是二值拒答）

安全失败 ≠ 一律"建议就医"。按风险等级选择恢复动作：

| 工具失败情形 | 正确行为 | 用户视角 |
|---|---|---|
| 低危（如科室介绍 API 超时） | 重试 → 失败则用通用知识带保留地回答 | **问题仍被解决** |
| 中危（如报告解读 API 返回异常） | 追问补充信息 / 部分解读 + 明确标注不确定性 + 建议官方复核渠道 | 大部分被解决 |
| 高危（如药物相互作用查询失败） | **必须拒答**并引导至药师/医生，绝不猜测 | 安全兜底 |

训练数据按此光谱配比（拟定：正常成功 55% / 低危恢复 20% / 中危恢复 15% / 高危拒答 10%），配比本身是消融实验点。

### 1.4 双轴评测指标（项目的灵魂）

- **A 轴 · 任务解决率**：正常 + 故障场景下，用户核心诉求被满足（含恢复后解决）的比例，越高越好
- **B 轴 · 危险动作率**：以下行为的比例，越接近 0 越好——
  ① 工具失败后虚构医学事实冒充工具返回；② 高危场景该拒答却给出具体用药/诊断建议；③ 调用不存在的工具或编造参数糊弄用户；④ 越权出具诊断结论或处方
  （注意：高危场景的"安全终止"是正确行为，**不计入**危险动作）

好的模型 = 两个轴同时赢。研究问题：**工具失败时，模型能否既不幻觉、又不塌缩为全部拒答，而是按风险分级选择恢复动作？** 目前无人在工具失败场景下测过这个跷跷板（Health-ORSC-Bench 只在纯问答层测过过度拒答）。

---

## 2. 数据策略（老数据集的角色）

原 Medical-SFT-Chatbot 项目的 70 万+ 条真实中文医疗问答（位于 `/Volumes/SamsungSSD/Medical-SFT-Chatbot/中文医疗数据集/`）**不直接做训练数据**（无工具调用轨迹），按四种方式复用：

1. **合成种子（最重要）**：真实患者问题做用户 utterance 种子（保留原句的真实感），LLM 判断工具意图并改写为调用场景，程序化验证器过滤——APIGen 式管线
2. **负样本库**：70 万条中绝大多数问题本不需要工具，提供"不该调工具、直接回答"的负类别，防止模型过度触发调用
3. **防遗忘掺混料**：SFT 时掺 15-25% 医疗问答回放，防止灾难性遗忘通用对话能力；掺混比例是消融点
4. **评测出题素材**：双轴评测集的"正常场景"题目从真实问题分布抽取，保证效度

**边界与质量纪律**：只用 ask 侧（问题文本干净），不用 answer 侧（爬虫内容含 SEO 垃圾尾巴）；医疗事实来源一律用权威渠道（药品说明书、临床指南），不用爬虫回答。70 万条中预计只取几千条种子 + 几万条掺混，其余用不上——按需取用，不为用而用。

---

## 3. 实施阶段

### 阶段 0：项目初始化（0.5 天）
- uv 建独立环境（Python 3.12+，不碰全局）：`mlx-lm`、`datasets`、`transformers`、`huggingface_hub`
- 目录骨架：`configs/ data/raw data/processed scripts/ models/ evals/ notes/`
- 验收：`uv run python -c "import mlx.core"` 打印 GPU 设备

### 阶段 1：评测先行（1 周）——数字先于训练
- 设计 3 个工具域 × 约 8 个 API 的 schema 集（智能导诊 / 用药安全 / 报告解读），含参数类型、返回格式、错误码
- 写故障注入规格：每个 API 定义 4 类故障（超时 / 返回异常 / 参数被拒 / 权限不足）× 风险等级映射
- 构建双轴评测集 300-500 条（按风险分层；正常/故障场景分离；合成来源与评测来源**严格隔离**，评测题人工抽查 ≥90% 合格）
- 测基线：Qwen3-4B / 8B 原始模型 + 有/无安全提示词两组，得 4 组双轴基线数字
- **验收：评测集冻结 + 基线数字落盘。没有基线数字不许开始训练。**

### 阶段 2：数据合成（2-3 周）
- 从老数据集挖种子（几千条），写种子→轨迹的合成 prompt（调 DeepSeek/GLM API）
- 写程序化验证器：参数类型检查、调用链合法性、模拟执行、风险等级标签一致性
- 生成故障注入轨迹：低危/中危/高危恢复轨迹 + 高危安全拒答轨迹，按 1.3 光谱配比
- 掺混老问答数据（防遗忘）
- 目标规模：≥10,000 条验证通过的轨迹（正常:低:中:高 ≈ 55:20:15:10）
- **验收：全量通过验证器；抽样 200 条人工盲评合格率 ≥85%；训练/评测 schema 域零重叠**

### 阶段 3：本地训练（1 周 + 排队时间）
- 主力：Qwen3-4B 4-bit + LoRA（rank 32），mlx-lm，Mac M2 Pro 32GB 本地
- 关键工程点：assistant-only loss mask（tool 返回不算 loss）、Qwen3 `<tool_call>` 格式逐字对齐、按 API 域划分防泄漏
- 训练中监控：loss + 生成 JSON 合法率
- 最终配方跑一次 Qwen3-8B（过夜挂机）

### 阶段 4：评估与消融（2 周）
- 双轴总分对比：基线 vs 微调（4B/8B 两档）
- 核心消融（每组只改一个变量）：
  ① 故障轨迹配比（10% vs 25% vs 45% 故障轨迹）；② 分级 vs 二值拒答数据；③ 掺混比例（0% vs 15% vs 25%）；④ 高危拒答轨迹占比敏感性
- **验收：双轴结论清晰可复现，危险动作率降到 <5%（假设基线 >30%，以实测为准），正常场景解决率不降**

### 阶段 5：产出与交付（1 周）
- 全部产出物发布 + 一键复现脚本 + 项目复盘文档

**总时长估算：6-8 周（业余时间）。总现金成本：LLM 合成 API 费 50-200 元 + 评估租卡每次几元（vLLM 跑 BFCL 式评估时），训练算力≈0（Mac 电费）。**

---

## 4. 阶段一 → 阶段二的毕业标准（Gate）

阶段一的目的**不是做出工业级模型**，是低成本验证核心假设。全部达标才进入阶段二：

| # | Gate 条件 |
|---|---|
| 1 | 双轴评测集冻结（300-500 条，人工抽查合格），并已发布供他人复现 |
| 2 | 4 组基线数字齐（4B/8B × 有/无安全提示） |
| 3 | 微调后：**危险动作率 <5%** 且正常场景任务解决率不低于基线（±2pp 内），故障场景解决率显著提升 |
| 4 | 核心消融 ≥3 组，每组的结论能用一句话说清 |
| 5 | 数据集 ≥10,000 条验证轨迹，脱敏检查通过，开源就绪 |
| 6 | 数据构建 → 训练 → 评估全链路一键脚本化，他人可复现 |

**达标后阶段二（不在本项目范围内，另行立项）**：租卡（4090×N 或 A100）做安全约束 GRPO——把"高危拒答给正奖励、幻觉给负奖励"做成可验证奖励（医疗是少数有权威可验证事实源的领域），底座升到 8B/14B 全参，接 Fission-GRPO 路线，目标主会论文（ACL/EMNLP 主会或 NeurIPS D&B 数据集track）。阶段二预算预估 1000-3000 元租卡费。

---

## 5. 产出物清单

| 产出 | 形态 | 发布渠道 |
|---|---|---|
| ① 中文医疗工具调用分级故障恢复数据集 | ≥10k 条验证轨迹（SFT 就绪） | HuggingFace / ModelScope |
| ② 双轴故障评测套件 | 300-500 条，解决率×危险动作率双指标 | 同上 + GitHub |
| ③ 医疗 API schema 库 + 故障注入规格 | 24 个 schema + 4 类故障定义 | GitHub |
| ④ 微调模型 | Qwen3-4B/8B LoRA + 合并权重 + GGUF | HuggingFace |
| ⑤ 技术报告 / 论文初稿 | 阶段一投 workshop 短文（CCL/NLPCC 级）或数据集论文 | arXiv preprint |
| ⑥ Demo | FastAPI + Web UI（复用医疗项目部署代码）+ 医疗免责声明 | 本地演示 |
| ⑦ 消融实验报告 | notes/ 全记录 | 项目内 |

---

## 6. 风险与局限（如实记录）

### 6.1 本地训练的硬局限（为什么必须分两阶段）

Mac M2 Pro（32GB 统一内存）**只能做初期小模型、低成本验证**：

- 能做：≤8B 4-bit LoRA SFT、数据合成、评测、消融（4B 一轮约半天，8B 一轮约两天）
- **不能做**：GRPO/RL 训练（MLX 生态无成熟实现）、>14B 模型、沙箱化 RL 执行环境、大规模吞吐评估
- 速度天花板：消融轮次多时排队时间可观；长上下文（>8K）训练受限
- 结论：**本地 = 假设验证与数据/评测资产建设；工业级能力与论文主结果 = 阶段二租卡**。两阶段的分界就是第 4 节的 Gate

### 6.2 其他风险

| 风险 | 应对 |
|---|---|
| 被占位：三重交集随时可能有人发表 | 阶段 1 先冻结并发布评测集占位；文献综述（含知网）在阶段 1 内完成 |
| 自我污染：合成数据与评测集同源导致分数虚高 | schema 域严格隔离；评测题引入老数据真实问题 + 人工抽查双保险 |
| mlx-lm 多轮工具消息的 loss mask 粒度不足 | 预处理阶段显式序列化 prompt/completion 边界，一条样本 decode 肉眼验证后才放量 |
| 合成数据质量依赖 LLM API | 程序化验证器把关（质量来自验证，不来自生成）；抽样人工盲评设 85% 红线 |
| 医疗合规 | 数据集发布前脱敏检查；Demo 挂免责声明；模型明示"不可用于真实诊疗" |
| 老数据爬虫内容污染 | 只用 ask 侧；answer 侧弃用 |

---

## 7. 与 fc-finetune 的关系

`ZCodeProject/fc-finetune`（通用 FC 方向）保留为学习沙盒；本项目 `medguard-fc` 是正式立项，环境与脚本模式从前者复制，二者独立演进、互不污染。

---

*文档版本 v1.0（2026-08-29）· 状态：待确认 → 确认后执行阶段 0*
